A paragraph with inline math: $E = mc^2$ and more text.

Another with $x^2 + y^2 = z^2$ embedded.

Block math:

$$ \\sum\_{i=0}^{n} i = \\frac{n(n+1)}{2} $$

Text after block math.

$$ \\int_0^1 x^2 dx $$

Final paragraph.